import { observer } from 'mobx-react-lite'
import { Page } from '@/components/core'


export const UsersPage = observer(() => {
    return (
        <Page>
            <div> Users </div>
        </Page>
    )
})
